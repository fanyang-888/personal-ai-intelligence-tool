[build]
builder = "NIXPACKS"

[deploy]
startCommand = "alembic upgrade head && python -m scripts.seed_sources && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
