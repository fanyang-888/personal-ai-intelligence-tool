services:
  postgres:
    image: postgres:16-alpine
    container_name: pait-postgres
    environment:
      POSTGRES_USER: pait
      POSTGRES_PASSWORD: pait
      POSTGRES_DB: pait
    ports:
      - "5432:5432"
    volumes:
      - pait_pg_data:/var/lib/postgresql/data

volumes:
  pait_pg_data:
